---
phase: 01-combined-compass-library
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/App.jsx
autonomous: true

must_haves:
  truths:
    - "Navigating to /results renders CombinedPage (not old Compass)"
    - "Navigating to /library redirects to /results"
    - "Navigating to / renders CombinedPage"
    - "compassVersion key still remounts CombinedPage on version change"
  artifacts:
    - path: "src/App.jsx"
      provides: "Updated routing"
      contains: "Navigate to=\"/results\""
  key_links:
    - from: "src/App.jsx"
      to: "src/pages/CombinedPage.jsx"
      via: "import CombinedPage"
      pattern: "import CombinedPage"
    - from: "/library route"
      to: "/results"
      via: "Navigate replace"
      pattern: "path=\"/library\".*Navigate"
---

<objective>
Wire CombinedPage into the router and redirect /library to /results.

Purpose: CombinedPage is built but not yet reachable. This plan makes it the live page at /results and /library, completing the phase.

Output: Updated App.jsx with CombinedPage at /results and /, /library redirect to /results.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-combined-compass-library/01-01-SUMMARY.md
@src/App.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update App.jsx routing to use CombinedPage</name>
  <files>src/App.jsx</files>
  <action>
Edit src/App.jsx:

1. ADD import at top (after existing imports):
   ```jsx
   import CombinedPage from "./pages/CombinedPage";
   ```
   Keep the existing `import Compass` and `import Library` lines for now (they are still exported files — removing them is optional cleanup).

2. CHANGE the /results route (currently lines 102–109):
   ```jsx
   <Route
     path="/results"
     element={
       <HelpGuard>
         <Layout><CombinedPage key={compassVersion} /></Layout>
       </HelpGuard>
     }
   />
   ```
   Replace `<Compass key={compassVersion} />` with `<CombinedPage key={compassVersion} />`.

3. CHANGE the /library route (currently lines 79–85):
   ```jsx
   <Route
     path="/library"
     element={<Navigate to="/results" replace />}
   />
   ```
   Remove the HelpGuard+Layout wrapper — this is now a pure redirect. The /results route already has HelpGuard.

4. CHANGE the / (root) route (currently lines 70–77):
   ```jsx
   <Route
     path="/"
     element={
       <HelpGuard>
         <Layout><CombinedPage key={compassVersion} /></Layout>
       </HelpGuard>
     }
   />
   ```
   Replace `<Library />` with `<CombinedPage key={compassVersion} />` so root also shows the combined page.

5. Ensure Navigate is already imported from react-router (it is — line 1 of App.jsx). No new imports needed beyond CombinedPage.

Do NOT remove the Compass or Library imports yet — they may be referenced elsewhere and removal is safe cleanup for a follow-up, not required for this phase.
  </action>
  <verify>
    Run: `cd "C:/EV-CompassV2" && npm run build 2>&1 | tail -20`
    Expect: Build succeeds with exit code 0, no errors.
    Also run: `cd "C:/EV-CompassV2" && npm run dev &` then `curl -s http://localhost:5173/ | head -5` (or just verify build passes — dev server check is optional).
  </verify>
  <done>
    App.jsx imports CombinedPage, /results route uses CombinedPage, /library route is a Navigate redirect to /results, / route uses CombinedPage. `npm run build` exits 0.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Combined compass+library page is now live at /results. The full radar chart renders at top with compare panel. Lens icon badges (green house, orange gavel) appear in the chart's top-left corner. Below the compass: draggable pill strip, search bar, and topic cards grid. /library redirects to /results.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:5173/results
    2. Verify radar chart (or BuildCompassPrompt if uncalibrated) renders at top
    3. If calibrated: confirm green house icon and orange gavel icon appear in top-left of chart area
    4. Click the green house icon — confirm CalibrationOverlay opens immediately (no page navigation)
    5. Click the orange gavel icon — confirm CalibrationOverlay opens immediately
    6. Scroll down — confirm pill strip, search bar, and topic cards grid are visible below the compass
    7. Click a topic tile — confirm it toggles on/off the compass (purple pill appears/disappears)
    8. Navigate to http://localhost:5173/library — confirm it redirects to /results
    9. Check dark mode toggle — confirm full dark theme works on combined page
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks correct, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` exits 0 (no build errors)
2. App.jsx contains `import CombinedPage` 
3. /results route uses `CombinedPage key={compassVersion}`
4. /library route contains `Navigate to="/results" replace`
5. / route uses `CombinedPage key={compassVersion}`
</verification>

<success_criteria>
- npm run build passes cleanly
- /results renders CombinedPage (not old Compass)
- /library redirects to /results (no longer renders Library component)
- / renders CombinedPage
- Lens icon badges open CalibrationOverlay directly without navigation
- Library section visible below compass on /results
- Dark/light mode works on combined page
- compassVersion key preserved for remount behavior
</success_criteria>

<output>
After completion, create `.planning/phases/01-combined-compass-library/01-02-SUMMARY.md`
</output>
